export interface Empresa {
    id: string
    nome: string
    nuip: string
    pais: string
    cidade: string
    endereco: string
    telefone: string
    email: string
    pessoa_contato?: string
    padrao: boolean
}